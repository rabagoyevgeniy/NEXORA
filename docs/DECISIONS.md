# NEXORA — Журнал решений

Формат: дата — решение — обоснование. Решения не пересматриваются без записи о пересмотре.

## 2026-08-12

**D-001. Жанр: текстовая PvP-дуэль на LLM-движке («Грамматика»).**
Обоснование: единственный жанр, структурно недоступный большим студиям И совместимый с workflow «промпты с телефона в Cursor».

**D-002. LLM — переводчик, не судья.**
LLM: фраза → структура. Исход считает детерминированный код. Причина: защита от prompt injection + гарантия честности.

**D-003. Ходы одновременные, вскрытие как в покере.**
Причина: снимает латентность LLM, снимает реалтайм-нетворкинг, добавляет чтение противника.

**D-004. Прекаст обязателен.**
Черновик разбора до отправки. Причина: убивает главный риск — непредсказуемость собственных действий.

**D-005. Эрозия заклинаний.**
Частота использования фразы в мире ↑ → цена ↑. Первооткрыватель — перГолосентная скидка. Причина: анти-мета, встроенная в экономику.

**D-006. Лимит фразы — 12 слов.**
Причина: поэзия, а не эссе; защита от «кто напишет длиннее»; контроль стоимости инференса.

**D-007. Web-only, Vercel, React+TS+Supabase+Anthropic API.**
Причина: разработка и проверка с телефона; переиспользование паттернов Null Club / CoachFlow.

**D-008. Документация концепта живёт в этом репо, коммитит только Cursor.**
Причина: правило «один агент на репозиторий» (урок Aqua Trench). Ideation — в чате Claude, файлы переносятся промптом.

**D-009. Первая проверка фана — дуэль против ИИ-противника, не hot-seat.**
Причина: тестируется в одиночку, из любого места.

## 2026-08-13 (итерация 3 — экономика)

**D-010. Деньги покупают пространство для творчества, не силу.**
Стержневой принцип монетизации. Голос не продаётся никогда.

**D-011. Дыханияа — мета-ресурс на новые заклинания.**
Новая фраза = платный инференс = Дыханияа. Известная = кэш = бесплатно. Игровой ресурс зеркалит себестоимость. Дневной бесплатный запас = потолок расходов разработчика на игрока.

**D-012. Рейтинг изолирован от денег.**
Бюджет новизны в рейтинговом матче одинаков для всех (3 новых заклинания/дуэль), Дыханияа в рейтинге не действуют. Платное преимущество = только знание из Эха («шахматный учебник», не «лишний ферзь»).

**D-013. Роялти Дыханием за адопцию.**
Использование твоего заклинания другими начисляет тебе Дыханияа. Творческое ядро выходит на самоокупаемость by design.

**D-014. Открытие регистрируется только в матче против человека.**
Эхо (песочница) не открывает заклинания. Защита от бот-фарма открытий + кластеризация эмбеддингов против сквоттинга вариаций.

**D-015. Модель дохода: подписка + разовая косметика + сезонные тома.**
Подписка «Кодекс» ~$6/мес. Конкретные числа — после данных прототипа (Q-08).

## 2026-08-13 (итерации 4–7 — мета-слой)

**D-016. ПЕРЕСМОТР D-005: роялти-скидка первооткрывателя → Сигнатуры.**
Найдено противоречие D-005 × D-012: перГолосентная скидка = вертикальная прогрессия, протащенная в честный рейтинг (старые аккаунты > лучшие формулировщики). Замена: автор «подписывает» открытие → иммунитет к эрозии (базовая цена навсегда), лимит слотов печатей 3→7–9, рост слотов только игрой. Роялти Дыханием (D-013) сохраняется без изменений.

**D-017. Прогрессия — в ширину, не в высоту.**
Никакого роста цифр урона/HP. Рост = слоты сигнатур, школы, модификаторы, знание. Заработанная ширина легитимна, купленная — нет.

**D-018. Открытия контекстуальны: заклинание × среда.**
Новая арена = новая целина открытий. Среды обязательны; сезонная арена = возобновляемый источник «золотой лихорадки» для поздних игроков. Направление Q-02 предрешено.

**D-019. Выцветание печатей.**
Заклинание без использования сезон → активная печать спадает, доступно переоткрытие. Летопись первого автора вечна (слой истории), печать циклична (слой власти). Аналог: рекорд Болта вечен, чемпион мира — сегодняшний.

**D-020. Q-03 ЗАКРЫТ: асинхрон — базовый режим.**
Асинхронные дуэли (одновременные ходы, резолв по готовности обоих) = ядро мобильного retention + решение матчмейкинга малой базы. Синхронная дуэль = тот же движок в быстром темпе, не отдельный режим.

**D-021. MVP retention-слой = асинхрон + дневные Дыханияа. Точка.**
Контракты v1.1, турниры v1.2, сезоны при живой базе. Анти-scope-creep для соло-разработки.

**D-022. Эволюция без патчей.**
Эрозия двигает мету сама; сезонные арены = твики тегов среды; мировые события = твики коэффициентов детерминированного слоя.

## 2026-08-13 (итерации 8–10 — запуск и психология)

**D-023. Эпоха Безмолвия + Открытие Гримуара как событие запуска.**
Предсезон: игра играбельна, Гримуар запечатан. Решает холодный старт (Q-09) без ослабления D-014; даёт окно ред-тиминга (Q-04) и калибровку эрозии (Q-05). Дата Открытия — по метрике живой базы.

**D-024. Открытия — только в matchmade-дуэлях.**
Вызовы друзей играбельны, но не регистрируют открытия (анти-сговор). ИИ-матчи не регистрируют никогда. UI обязан явно маркировать нерегистрирующие дуэли.

**D-025. Судья нем в вопросах качества.**
Язык системы — только физика. Голос = масштаб, не оценка. Системная похвала запрещена в обе стороны; признание — только от людей.

**D-026. Экран поражения начинается с добычи.**
Увиденные заклинания → записная книжка с разбором. Проигрыш = разведка. Побочная петля: распространение заклинаний победителя через проигравших → роялти (D-013).

**D-027. Культ КПД.**
Эффект/Голос — подсвечиваемая метрика мастерства. Косметическая слава, без наград силой.

**D-028. Никаких скрытых поддавков.**
Матчмейкинг по открытому рейтингу — единственный регулятор сложности. Pity-механики запрещены.

**D-029. Арена как муза.**
Свойства среды видимы как слова-аффордансы: источник идей против чистого листа + декларация физики арены. Подсказки готовыми фразами запрещены.

## 2026-08-13 (итерации 11–13 — онбординг и воронка)

**D-030. Time-to-magic < 60 сек.**
Холодный старт в сцену без регистрации/меню/синематиков. Гостевой аккаунт молча; имя — после первой дуэли (идентичность после инвестиции).

**D-031. Мир всегда отвечает.**
Состояния «invalid input» не существует: любой ввод резолвится минимум в шёпот (0 эффекта, 0 Голоса, 0 стоимости). Оба тира ввода видны с нулевой секунды; порядок (свободный vs осколки) — A/B прототипа.

**D-032. Инициация — открытый наставник.**
Дух объявляет себя учителем; условие завершения — первое эффективное заклинание игрока; поражения нет. Скрытых поддавков нет по построению (согласовано с D-028). Контент духа: ~20 скриптованных битов + LLM в жёсткой персоне.

**D-033. УТОЧНЕНИЕ D-023: расслоение кэша и летописи.**
Инфраструктурный кэш фраз работает с первого дня (независим от Безмолвия). Безмолвие запечатывает только летопись (имена/печати/роялти). Закрыта дыра: предсезон был бы периодом максимальной себестоимости инференса.

**D-034. Реплей-страница = воронка.**
Каждый шаренный реплей заканчивается полем «Скажи своё слово» → бесшовный вход в Инициацию. Требуется rate-limit по IP (→ Q-04).

**D-035. Выход из онбординга с открытой петлёй.**
Первая matchmade async дуэль в очереди + видимый таймер Дыхания. Ожидание заполняется Эхом/вторым духом; матчмейкинг приоритетно сводит новичков.

## 2026-08-13 (итерации 14–17 — социальный слой, модерация, аудит; ФАЗА 1 ЗАКРЫТА)

**D-036. Модерация «мир отвергает скверну».**
Safety-класс в том же вызове парсинга (нулевая стоимость); отказ мира — легитимный тип ответа (совместим с D-031); прекаст = приватное превью модерации с нулевой ценой ложного срабатывания; ворота публикации строже приватного резолва; двухступенчатый парсинг защищает бюджет.

**D-037. Восхищения: одно в день.**
Дефицит делает похвалу весомой и создаёт дневной ритуал. Запуск — на Открытии Гримуара. Система по-прежнему нема (D-025) — восхищаются только люди.

**D-038. Театр реплеев (v1.1).**
Автокурируемая лента резолвов по КПД/новизне/восхищениям. Без человека-редактора.

**D-039. Родословная (v1.1).**
Referral-tracking в лоре генеалогии: «первое слово» + древо линий на странице ветерана. Чисто косметика, ноль экономических наград.

**D-040. Школы-знамёна (v2).**
Тег + полка сигнатур + сезонная лига школ. Внутреннего чата не будет никогда — модерационная ноша отдана внешним инструментам.

**D-041. Отклонено навсегда: ставки, кооп-каст, встроенный чат.**
Гэмблинг-риск / скоуп / модерация. Записано, чтобы будущие «а давайте» разбивались о документ.

**D-042. Уточнения финального аудита.**
(а) КПД — публичная арифметика, не системная похвала: граница D-025/D-027 зафиксирована. (б) Инициация: после N шёпотов дух подводит к осколкам — тупик невозможен. (в) Новизна для Дыхания = промах инфраструктурного кэша, не отсутствие в летописи.

**ФАЗА 1 (мета-слой) ЗАКРЫТА: 17 итераций, D-001…D-042, 6 дыр найдено и заделано.**

## 2026-08-13 (итерация 18 — аудит v2; найдено 8 пунктов после «закрытия» фазы)

**D-043. Ключи эрозии = ключам открытий (кластер × среда).**
Рассинхрон систем координат устранён: заезженность фразы в одной арене не дорожит её в другой.

**D-044. Эмиссия роялти затухает с эрозией.**
Незакрытая инфляция Дыхания устранена тем же механизмом, что двигает мету: чем заезженнее фраза, тем меньше капает автору. Числа → Q-08.

**D-045. Стилевые бонусы — только за механически проверяемую форму.**
Слоги/структуру считает детерминированный код. LLM-оценка красоты запрещена (граница D-025 распространена на хардкор-тир; противоречие хайку-бонуса устранено).

**D-046. Цена фиксируется в момент отправки хода.**
Эрозия между отправкой и асинхронным резолвом не трогает уплаченное. Прекаст-контракт (D-004) нерушим во времени.

**D-047. Рейтинг открывается не раньше Открытия Гримуара; название игры — Nexora.**
(а) В Безмолвии холодный кэш душил бы бюджет новизны рейтинга. (б) Q-07 закрыт: Nexora — имя продукта, «Грамматика» — кодовое имя механики.

**D-048. Язык: ввод любой, летопись едина по смыслу.**
Мультиязычные эмбеддинги объединяют переводы в одно открытие. Кросс-языковая точность — гипотеза Безмолвия (→ Q-05). UI: EN первым, RU вторым.

**D-049. Документная дисциплина.**
Найдена рассинхронизация CONCEPT.md с D-016 (устаревшая «перГолосентная скидка»). Правило: решение, отменяющее старое, обязано править ВСЕ упоминания в CONCEPT.md в том же коммите. CONCEPT.md — контекст для Cursor, противоречия в нём = баги в промптах.

**ФАЗА 1 ЗАКРЫТА АУДИТОМ v2: 18 итераций, D-001…D-049, 7 дыр фазы + 8 находок пост-аудита.**

## 2026-08-13 (итерации 19–22 — Фаза 2, таксономия; Q-14 закрыт)

**D-050. Таксономия = грамматика, не enum.**
Структура: {глагол, субстрат, аспекты[], цель, величина, длительность, [райдер]}. 10 глаголов × 6 субстратов × 5 целей = 300 архетипов. Пути «плоский enum» и «полная физика» отвергнуты.

**D-051. Величины — только ординальные классы.**
LLM выдаёт ярлыки {лёгкое…предельное}, {мгновенное…до рассеивания}; числа назначает код. D-002 распространён на все количественные поля.

**D-052. Аспекты — открытые теги без логики.**
Модулируют коэффициенты и визуал, никогда не ветвят правила. Открытость словаря безопасна по построению.

**D-053. Райдер: максимум один, триггеры закрыты.**
{при-движении, при-касте, при-контакте, в-конце-хода}. Комбинаторный взрыв условий отрезан.

**D-054. Резолв — фиксированные слои, не пары.**
слово → пространство → оградить → преобразить → сковать/скрыть → нанести/истощить/влить → райдеры → тик. Порядок = физика мира; лог резолва обучает слоям.

**D-055. Язык и физика — разные слои ценности.**
Летопись ключуется семантикой, движок — структурой; совпадение физики не сливает открытия. Коллапс иллюзии структурно исключён; эрозия получает клапан «новые слова для старой физики».

**D-056. Аффинность среды = модификатор цены по аспектам.**
Мета локальна по аренам; защёлкивается с D-043.

**D-057. Дисциплина заморозки.**
Глаголы/субстраты закрыты навсегда, изменения аддитивны (version в структуре), сезоны добавляют арены/аспекты — не логику.

## 2026-08-13 (итерации 23–26 — контракт LLM↔код; Q-15 закрыт)

**D-058. ПЕРЕСМОТР «дешёвого прекаста»: единственный парс, коммит по хэшу.**
Двухмодельная схема ломала D-004 (согласие с A, исполнение B). Черновик = полный разбор; коммит = хэш; повторного разбора нет. Бюджет защищает ступень-1 микроклассификатор + кэш. Формулировки в CONCEPT.md исправлены этим же коммитом (D-049).

**D-059. Схема структуры v1.**
Полей с числами нет; все закрытые enum'ы + открытые аспекты-теги + safety + confidence. Спека в COMBAT-CORE.md.

**D-060. Одна трактовка; переписывание = уточнение.**
Меню вариантов запрещено. confidence: low → подсказка. Точность формулировки — продаваемый скилл.

**D-061. Грамматика = песочница инъекций.**
Канал LLM→движок не имеет степеней свободы кроме легальных enum'ов; максимум атаки — дорогой легальный класс. D-051 ретроспективно признан решением безопасности. Гигиена: UTTERANCE-изоляция, zod, 1 repair, мета-фразы → шёпот.

**D-062. Отказы без потерь.**
Ретраи → черновик сохраняется, потерь нет. Машина состояний draft→commit(hash)→resolve; резолв — чистая идемпотентная функция.

**D-063. Канонизация кластеров.**
Первый разбор кластера = его структура навсегда; наследование без парса. Недетерминизм LLM устранён. Лор: «первое слово формирует заклинание».

**D-064. Нет RNG.**
Неопределённость только из скрытых одновременных ходов. Шахматы+покер. Честность D-028 дотянута до костей резолва.

**D-065. Нарратив — детерминированные шаблоны.**
LLM отсутствует в пути резолва. Ноль стоимости, воспроизводимость, переводимость.

## 2026-08-13 (итерации 27–30 — состояние дуэли и архитектура; Q-16, Q-17 закрыты)

**D-066. Стойкость — единственная шкала жизни.**
Мульти-полоски отвергнуты ради читаемости. 0 = поражение.

**D-067. Голос видима; разум = информационная война.**
Публичный стек = пища чтения + телеграф накоплений. скрыть/разум — маска ±класс; нанести/разум — в обход физических щитов; сковать/разум — лимит слов 12→8/6 (атака на само оружие игры, причина видима жертве).

**D-068. Единая ординальная арифметика.**
Щит поглощает ≤ своей величины, выше пробивает разницей; рассеять снимает ≤; сковать держится величиной. Одно правило — все слои.

**D-069. Пространство = состояния, не координаты.**
Якоря магов + флаг высоты + центр. Сетка отвергнута; расширение только аддитивно при доказанной нужде прототипом.

**D-070. Победа/ничья/время.**
Стойкость 0; обоюдный 0 = сплит; асинхрон 48ч/ход → сдача; синхрон 60с → пас (Голос копится), два паса = сдача. Красный флаг баланса: сковать/слово.

**D-071. Сервер-авторитарность + RLS-одновременность.**
Клиент не вычисляет исходы; закоммиченный ход противника недоступен из БД до двойного коммита (политика строк). Урок CoachFlow применён превентивно.

**D-072. UX: дуэль = переписка.**
Инбокс дуэлей как чаты; резолв = сообщение мира; ввод внизу. Асинхрон получает родной паттерн мессенджера.

## 2026-08-13 (итерация 31 — финальный аудит Фазы 2; 9 находок)

**D-073. Экономика черновиков.**
Лимит промахов кэша на ход (известные кластеры — бесплатно/безлимитно); цена в хэше черновика с TTL ~10 мин; бюджет новизны считает коммиты. Незащищённый фланг рейтинга закрыт.

**D-074. Печать = кластер × среда.**
Уточнение связки D-016/D-018/D-043: слоты сигнатур считают пары.

**D-075. Воплощённые сущности — та же арифметика.**
Барьер = щит для обоих; hazard = условный удар. Новых правил нет.

**D-076. Внутридуэльное утомление.**
Повтор кластера в одной дуэли: +1 класс цены за повторение. Анти-стал, анти-спам, принудительная вариативность. Эрозия стала фрактальной (мир ↔ бой). Сигнатуры не спасают от локальной усталости.

**D-077. Наблюдение через RPC.**
Состояние дуэли — только через функцию с инфо-эффектами наблюдателя; прямой select противника запрещён RLS. + unique constraint идемпотентности резолва. (Маска разума перестала быть декорацией.)

**D-078. Стилевой бонус — косметика в MVP.**
Ореол/отметка формы; механическая скидка — эксперимент Безмолвия под красным флагом.

**Правка CONCEPT.md**: кор-луп п.5 приведён к структуре без чисел (второй рецидив рассинхрона → в ритуал аудита добавлен grep запрещённых формулировок).

**ФАЗА 2 (боевое ядро) ЗАКРЫТА АУДИТОМ v2: итерации 19–31, D-050…D-078.**

## 2026-08-13 (итерации 32–33 — стресс-тест дичи + аудит v3 Фазы 2)

**D-079. Дичь конвертируется, не цензурируется.**
Творческий абсурд маппится в физику; визуал — из библиотеки примитивов (неизвестный аспект → generic); мем живёт в тексте фразы. Судьи «серьёзности» не существует (защита D-025).

**D-080. Лимиты и ворота публикации расширены.**
12 слов И 120 символов; в публикацию не проходят реальные имена, URL, хэндлы (приватный резолв работает). Поп-культура легальна; юр-осторожность → Q-19.

**D-081. Роялти — только из matchmade + дневной кап.**
Закрыт сговор-майнинг Дыхания через вызовы друзей. Граница симметрична открытиям (D-024).

**D-082. Каноническая формула цены.**
Единый порядок модификаторов; прекаст показывает разложение цены (обучение экономике интерфейсом).

**D-083. Маска Голоса конкретизирована.**
лёгкое ±2 / среднее ±4 / тяжёлое «?». Ложь, не туман.

**D-084. Щит ловит свой канал.**
Субстрат = канал воздействия; универсального щита нет; аспектные модификаторы щитов — не в MVP.

**D-085. Реплей = лог + версионированный рендерер.**
Кадры не хранятся; вечность реплеев стала дешёвой.

**ФАЗА 2 ЗАКРЫТА АУДИТОМ v3: итерации 19–33, D-050…D-085.**

## 2026-08-13 (итерация 34 — визуальный язык)

**D-086. Бой = панельная раскадровка (комикс-грамматика без комикс-продакшена).**
Три кадра на ход: реплики → столкновение → исход. Детерминированный векторный рендерер в комикс-композиции. Генеративный арт отклонён в MVP; премиум-эксперимент позже под красным флагом. Мем-формат «написал ЭТО → произошло ВОТ ЧТО» получил родную форму трёхпанельного стрипа.

## 2026-08-13 (итерация 35 — тиры визуала)

**D-087. Единый движок + тиры стилей; видео = «Кинематограф» по запросу.**
Раздельные движки по подпискам отклонены (фрагментация бренда/воронки, неравенство внутри дуэли); пер-ход видео отклонено навсегда (арифметика). Принято: стили внутри единого векторного движка (косметика D-015) + премиум-рендер избранных реплеев через Seedance/Higgsfield-пайплайн после запуска (лимит в топ-тире, модерация через ворота D-080, водяной знак = самоокупаемый маркетинг). Партнёрство Higgsfield — фаза после трекшна.

## 2026-08-13 (итерации 36–37 — Фаза 3 открыта: производство)

**D-088. Границы агентов.**
Claude владеет docs/ и корневыми контекст-файлами (AGENTS.md, README); Cursor владеет кодом. Взаимные правки запрещены. Секреты — только Vercel env.

**D-089. Лестница M0–M9 + гейт фана после M4.**
Движок первым (чистый TS = test-driven вслепую с телефона); летопись/печати/роялти — вне MVP-лестницы, включаются Открытием Гримуара. Гейт: 10 дуэлей с духом → нет желания 11-й → стоп проекта.

**D-090. DoD промпта: тесты + deploy + curl URL.**
Промпт без критерия проверки не существует. Сценарные тесты движка = исполняемая спецификация дизайна.

**D-091. Гипотезы в одном config.ts с тегом Q-08.**
Тюнинг Безмолвия без раскопок по коду.

## 2026-08-13 (итерации 38–39 — ФАЗА 4 «Мир и Голос» открыта; имя)

**D-092. ПЕРЕСМОТР D-047: игра — SLOVO.**
Слоган: «В начале было Слово» / "In the beginning was the Word" — механически истинен (D-063: первое слово формирует заклинание). «Слово»/«Word» как имя отклонено (поиск, домен/ТМ, ассоциативная тень); «Логос» отклонён (коллизия Logos Bible Software). Nexora — кодовое имя репо/студийных доков. Гейт перед Безмолвием: проверка доменов/ТМ.

**D-093. Событие запуска — «Первое Слово».**
Драматургия: в начале было Безмолвие — потом было Слово. Кампания = обратный отсчёт.

**D-094. Космология трёх китов + тон.**
Мир слушает / Гримуар — память мира / Слова устают. Лор проявлен из механик (людонарративное единство по построению). Тон: собственный миф о языке без божеств и доктрин; безличный Слух мира; стоическая конвертация юмора (D-079) как комедийный приём. Cursor-старт отложен решением владельца — Фаза 3 запаркована, PRODUCTION.md ждёт.

## 2026-08-13 (итерации 40–41 — Законы и обучающийся мир)

**D-095. НЕКСОРА — имя мира; SLOVO — имя игры.**
Иерархия: вселенная/репо/доки — Nexora, продукт — SLOVO. Поисковая связка «нексора slovo».

**D-096. Семь Законов Нексоры.**
Вся механика в семи пословицах; лор=туториал=маркетинг; дух произносит их в Инициации делом; Кодекс — первая страница Гримуара. Все семь — проекции существующих решений (кроме Закона 4 → D-098).

**D-097. Обучающийся мир: открыто и по закону.**
Четыре контура: экономический (реал-тайм по правилам), парсерный (few-shot bank, батчи, канонизация защищает старое), балансовый (только сезонно/объявленными событиями — эррата вслух), духи-зеркало меты (книга ИИ = живой Гримуар, ноль инференса). Тихие подкрутки запрещены Законом 5.

**D-098. Голод мира (Закон 4: «Забытое дешевеет»).**
Отрицательная ступень эрозии с полом на уровне категорий (субстрат × аспект × арена). Самобаланс экономики в обе стороны; дешёвые фронтиры для поздних игроков; публичные индикаторы голода = муза на данных без нарушения D-029. Осцилляции демпфируются кривыми/капами (числа → Q-08); спам-манипуляция самонаказуема.

## 2026-08-13 (итерации 42–43 — словарь и Отзвук)

**D-099. Словарь Нексоры: Чернила→Дыхание, Мана→Голос, Лаборатория→Эхо (цель — Отражение).**
Мир речи, не письма и не алхимии. Глобальный ренейм по всем докам (дисциплина D-049), grep-контроль склонений. Выдержали аудит: Гримуар, Стойкость, Печати, Осколки, Шёпот, Восхищения.

**D-100. Персона Отзвука + сценарий Инициации v1.**
18 битов дословно в WORLD.md; ~90 слов активного текста; Законы показаны делом. EN-перевод — гейт перед M8.

## 2026-08-13 (итерация 44 — workflow, дом Чернил, Пульс боя)

**D-101. Папка /prompts + цикл проверки.**
Промпты — файлами; команда Cursor — одна строка; Claude ревьюит диффы и гоняет тесты в своём контейнере (чтение публично — не зависит от токена); фиксы — prompts/P-XXX-fix.md.

**D-102. Дом Чернил.**
Игрок произносит Дыханием, мир записывает Чернилами. Чернила = вещество летописи + косметика страниц Гримуара. Печатание — интерфейс, не фикшн. Диктовка — в бэклог доступности.

**D-103. Пульс боя.**
Индикатор «подбирает слова», вскрытие с печатающейся фразой врага, драматургия низкой Стойкости, минимальный звук (5–6 SFX), серия «Непрерванная речь» (loss aversion), «На последнем дыхании» (лимит 16 при Стойкости ≤3, публично/симметрично — красный флаг). Тождество заклинаний подтверждено семантическим (кластеры, не строки) — точные повторы иррелевантны.

## 2026-08-13 (итерация 45 — реактивный бой)

**D-104. Замах Предельного [красный флаг].**
Extreme = двухходовое: видимый замах → удар. Реакция целым ходом с прекастом (уклон/щит/рассеивание замаха). Сет-пис вместо нюки; асинхрон-совместимо. Порождено запросом владельца «фаербол и 3 секунды на реакцию» — переведено на язык фундамента.

**D-105. Блиц.**
Опциональный синхрон, ход 20 сек; просрочка = пас, полный урон без крита. Настоящий тайм-прессинг без слома парс-латентности.

**Отказы итерации 45**: QTE-окна, карточки реакций, LLM-тайминг-судья, криты. Причины в COMBAT-CORE; ломают D-002/D-020/D-029/D-064/Закон 5.

## 2026-08-13 (итерации 46–47 — раны и двойное время)

**D-106. Раны: Очаги + память следа + падение как банкротство [красный флаг].**
Три очага (Опора/Хват/Взор), одно правило удорожания семейства глаголов, детерминированные колбэки источника раны, срыв действия через неплатёжеспособность Голоса. Внедрение v1.1. Отвергнуто: полоски по частям тела (D-066), судья/кубик падений.

**D-107. Часы угрозы + нарративное время.**
Реальное окно решения — детерминированная таблица из структуры угрозы; живёт в ответах на Замах (синхрон) и Блице; асинхрон — константа. Классы величин → словарь виртуального времени в шаблонах. Отвергнуто: LLM-время, переменные окна в асинхроне.

## 2026-08-13 (итерации 48–57 — hardening-раунд по запросу владельца)

**D-108. Печать — потолок цены, не фикс.** Автор ≤ любой чужой цены всегда.
**D-109. Канал произнесения Замаха.** Лимит 6 в ход выпуска; авто-выпуск; срыв рассеиванием сжигает Голос.
**D-110. Аддитивные лимиты слов (пол 4); серия только рейтинговая; ничья замораживает серию.** Найден и закрыт саботаж параллельного асинхрона серией.
**D-111. Кросс-языковая летопись: оригинал + курсив-толкование.**
**D-112. Кураторские затравки ~100 кластеров; locus — аддитивное поле v2; гонка Первого Слова по timestamp резолва; прекаст-UI топ-3+прочее; Блиц = экзамен эрудиции; Взор-рана = маска D-083.**

## 2026-08-13 (плейтест-сессия №1)

**D-113. Прекаст-прогноз воздействия.**
Черновик показывает чистый потолок эффекта в Стойкости + оговорку про срез щитами. Найдено живой игрой (разрыв «имя луны vs класс предельного»). Афоризм «Именем зовут — Голосом платят» — в сценарий Инициации.

**D-114. Пакет плейтеста №2 («почему скучно»).**
Однострочный черновик; «исполняй, потом учи» в первый час; ≤1 паса до предельного (Q-08); пас=«вдох» [флаг]; вражеский Замах в первом бою; стрип/звук = несущие; наставник ≤15 слов/ход; гейт фана валиден только на билде M4. Подтверждено игрой: конвертация дичи, покерное вскрытие, ментальные ловушки — живые.
